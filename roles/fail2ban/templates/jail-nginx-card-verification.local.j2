[nginx-card-verification]

enabled = true
filter = nginx-card-verification
port = http,https
logpath = %(nginx_access_log)s
findtime = {{fail2ban_card_verification_find_time}}
bantime = {{fail2ban_card_verification_ban_time}}
maxretry = {{fail2ban_card_verification_max_retry}}
